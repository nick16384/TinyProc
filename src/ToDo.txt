Future (optional) ideas:
- Simulate different access times
- Add muxers / demuxers
- External HW interfaces, Interrupts
- Caches, virtual memory, (segmentation / paging?) -> MMU

General hints / useful coding guidelines:
Mark methods with Debug_ when they are not part of the microarch and only there to provide debugging info.
Git issue regarding terminal not rendering input text after an exception:
https://github.com/dotnet/runtime/issues/110502
Temporary fix: Run "reset" after the error occurs
LibreOffice: Insert Ctrl + Enter (Page break) to instantly start a new page and make it independent of text in the previous page
Figure 1: CPU structure created with DrawIO
Figure 2: Memory layout created with https://github.com/gerph/memory-layout-diagram

Future (unplanned) ToDo's:
- Split instruction cycles into more logical clock cycles (= Add more NextClock() wait points)
-> Currently one continuous operation: Not realistic
- New meta instructions:
1. STORI (Store immediate value at address in register)
- Add bus for ALU Opcode (no register) instead of directly setting from CU.
- Make code C# language rule conform and as uniform as possible
-> Case of variables, preceding underscore, etc.
- Make bus easier to use by replacing adding uint[] DataAsWord fields, that throw an exception if the bus width is not 32.
- Fix errors when addresses in memory bus are set before the read enable, causing old data to be read when the read enable bit changes.
- Single bus access often results in multiple updates -> Fix. (Check e.g. MAR/MDR access or ALU computation count)
-> This is a result of bus members updating their content everytime an attached bus updates, even though the data is often redundant
   e.g. MemoryAddress and MemoryData get changed once each during memory writes, causing a double write.
   Potential fix: Enable write on setting EnableWrite only and not during bus updates.
- Add GitHub dependencies: Avalonia, Avalonia Hex Editor (AvaloniaHex), MessageBox.Avalonia
- Add source assembly syntax highlighting (enable/disable checkbox in Menu via "View > Enable syntax highlighting")

Upcoming / In progress ToDo's:
- Fix occasional TaskCanceledException on app exit
- Fix GUI errors popping up behind the main window

- Implement new assembly syntax (v3.0) [done]

- ASMv3: Fixes [in progress]
-> Make empty .data section possible (still requires definition, but does not need lines afterwards) [done]
-> Check no attributes work too [done]
-> TS: Every time a memory op needs to be done, shift target address if fixed
-> Implement attribute "inline = all" (expects only immediate values, does not create a .data section in memory, but instead
   writes immediate values directly into the operands of instructions)
-> When writing assembly in the GUI, use "direct" mode (instructions only, no .data section and header)
--> Optionally make header usable; When header detected, switch to full assembly mode (requires header, .data and .text)
-> Add constant value registers in assembly (for whatever reason?)
-> Revise "STORE" instruction: It requires an immediate and an address, however, only one of them fits in the 32 bit operand 2
-> Find out why the heck random instructions add 0x100 to their operands (e.g. jmp [textsection] in loader)
-> Add ajmp/rjmp/acall/rcall instructions (absolute / relative jump; Removes necessity to readjust memory locations;
   Assembler only needs to determine relative positions of labels and calls/jumps are always relative e.g. jmpr _label)
-> For AOPI, swap operands! (e.g. AB_SubtractionSigned => BA_SubtractionSigned)
- Fix up instruction execution definitions (use CopyRegReg() when possible, when logging use _currentInstruction.XYZ whenever possible)
- Centralize linking of instruction - type somewhere central (currently separated in assembler & Instructions class)

- Put ROM at top and not bottom (as per typical convention; Put reset vector at lowest ROM address)
- Implement stack and call, ret, int, and iret [done]
- Implement +[reserve: XYZ] for ASMv3 pointers
- Verify relative load/store working
-> Assembler should still respect absolute load addresses (when requested explicitly via __attribute__)
-> The loader needs to be rewritten

- Implement interrupt masking (maskable interrupts / non-maskable interrupts(NMIs))
-> NMIs still trigger if Interrupt Enable is not set
-> Trigger double fault when NMI is triggered when Interrupted flag is already set

- Add interrupt queue?

Changes for CPU 0.3-indev:
- Added stack and SP
- Moved IRB to Bus 2 (makes PC+IRB possible)
- Added relative / absolute jumps / calls
- CSOA went from B2 to B1
- Added relative load/store
- Memory layout changes:
  1. Move ROM to high addresses (Specify!)
  2. .data and .text sections loaded after another
- Remove B / AB opcodes (they are now pure aliases for JMP/AJMP), added ACALR and CALLR

Useless neglibly simple processor (UNSC)